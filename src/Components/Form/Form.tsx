import React from "react";
import Input from "../Input/Input";

const Form = () => {
  //   const { style } = props;
  return (
    <div style={{ textAlign: "center", fontSize: "24px" }}>
      <Input />
      <Input />
    </div>
  );
};
console.log(Input);

export default Form;
